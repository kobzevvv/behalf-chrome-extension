# Neon Database Connection String
# Replace with your actual Neon database connection string
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Example format:
# DATABASE_URL=postgresql://your-username:your-password@your-host.neon.tech/your-database?sslmode=require

# Cloudflare Worker URL (required for deployment)
CLOUDFLARE_WORKER_URL=https://your-worker.your-subdomain.workers.dev
